package eindberetning.it_minds.dk.eindberetningmobil_android.views;

public class ErrorDialogTest /*extends BaseTest<PairPhone>*/ {

/*
    public ErrorDialogTest() {
        super(PairPhone.class);
    }

    @Override
    public void runBeforeGetActivity() {
        MainSettings.getInstance(getActivity()).clear();
    }

    @Test
    public void testDialog() throws InterruptedException {
        String message = "error message here";
        final ErrorDialog dialog = new ErrorDialog(getActivity(), message);
        getActivity().runOnUiThread(new Runnable() {
            @Override
            public void run() {
                dialog.showDialog();
            }
        });
        solo.waitForDialogToOpen();
        TextView view = (TextView) solo.getView(R.id.error_dialog_view_error_message);
        assertEquals(view.getText().toString(), message);
        solo.clickOnView(solo.getView(R.id.error_dialog_view_ok_btn));
        solo.waitForDialogToClose();
    }
    */
}
